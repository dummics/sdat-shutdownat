<Window
    x:Class="Sdat.App.CriticalOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        x:Name="OverlayRoot"
        Padding="22"
        CornerRadius="12"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1">
        <Grid RowSpacing="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Spacing="4">
                <TextBlock x:Name="TitleText" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock x:Name="CountdownText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <ProgressBar Grid.Row="1" x:Name="CountdownProgress" Minimum="0" Maximum="100" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                <Button x:Uid="DismissButton" Content="Dismiss" Click="OnDismiss" />
                <Button x:Uid="SnoozeButton" x:Name="SnoozeButton" Content="Snooze 10 min" Click="OnSnooze" />
                <Button x:Uid="CancelButton" Content="Cancel" Style="{ThemeResource AccentButtonStyle}" Click="OnCancel" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
