<Window
    x:Class="Sdat.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="RootGrid">
        <NavigationView
            x:Name="ShellNav"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            PaneDisplayMode="LeftCompact"
            SelectionChanged="OnNavigationChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Overview" Tag="overview">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Home" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Schedule" Tag="schedule">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Clock" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Notifications" Tag="notifications">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Message" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Hotkey &amp; Tray" Tag="hotkey">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Keyboard" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Advanced" Tag="advanced">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Setting" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="About" Tag="about">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Help" /></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Grid>
                <ScrollViewer x:Name="OverviewView">
                    <StackPanel Padding="36,28" Spacing="20" MaxWidth="980" HorizontalAlignment="Left">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Overview" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Your active power schedules at a glance." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Padding="20" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                                <StackPanel Spacing="8">
                                    <TextBlock Text="One-time" Style="{ThemeResource SubtitleTextBlockStyle}" />
                                    <TextBlock x:Name="OneTimeStatusText" Text="Loading…" TextWrapping="Wrap" />
                                    <Button Content="Cancel" Click="OnCancelOneTime" HorizontalAlignment="Left" />
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Padding="20" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                                <StackPanel Spacing="8">
                                    <TextBlock Text="Daily" Style="{ThemeResource SubtitleTextBlockStyle}" />
                                    <TextBlock x:Name="DailyStatusText" Text="Loading…" TextWrapping="Wrap" />
                                    <Button Content="Cancel" Click="OnCancelDaily" HorizontalAlignment="Left" />
                                </StackPanel>
                            </Border>
                        </Grid>
                        <Button Content="New schedule" Style="{ThemeResource AccentButtonStyle}" Click="OnOpenSchedule" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="ScheduleView" Visibility="Collapsed">
                    <StackPanel Padding="36,28" Spacing="20" MaxWidth="720" HorizontalAlignment="Left">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Schedule" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Use a duration such as 36m, or a clock time such as 23:41." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="6">
                                <TextBlock Text="Power action" />
                                <ComboBox x:Name="ActionPicker" SelectedIndex="0" HorizontalAlignment="Stretch">
                                    <ComboBoxItem Content="Shut down" Tag="Shutdown" />
                                    <ComboBoxItem Content="Suspend" Tag="Suspend" />
                                    <ComboBoxItem Content="Restart" Tag="Restart" />
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="6">
                                <TextBlock Text="Repeats" />
                                <ComboBox x:Name="KindPicker" SelectedIndex="0" HorizontalAlignment="Stretch" SelectionChanged="OnKindChanged">
                                    <ComboBoxItem Content="Once" Tag="OneTime" />
                                    <ComboBoxItem Content="Daily" Tag="Daily" />
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                        <StackPanel Spacing="6">
                            <TextBlock Text="When" />
                            <TextBox x:Name="TimeInput" PlaceholderText="36m or 23:41" />
                        </StackPanel>
                        <ToggleSwitch x:Name="KeepDailyToggle" Header="Keep the daily schedule when times overlap" />
                        <Button Content="Schedule" Style="{ThemeResource AccentButtonStyle}" Click="OnSchedule" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="NotificationsView" Visibility="Collapsed">
                    <StackPanel Padding="36,28" Spacing="22" MaxWidth="720" HorizontalAlignment="Left">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Notifications" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Choose when SDAT warns you before a power action." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock Text="Reminder minutes" />
                            <TextBox x:Name="ReminderOffsetsInput" PlaceholderText="2 or 10, 2" />
                            <TextBlock Text="Up to five values, from 1 minute to 24 hours." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock Text="Daily overlap window" />
                            <NumberBox x:Name="DailyOverlapInput" Minimum="0" Maximum="1440" SpinButtonPlacementMode="Compact" />
                            <TextBlock Text="When a one-time action is this many minutes from the next daily action, SDAT skips that daily occurrence. Set 0 to disable." TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch x:Name="CriticalOverlayToggle" Header="Show the critical bottom-center overlay" />
                        <Button Content="Save settings" Style="{ThemeResource AccentButtonStyle}" Click="OnSaveSettings" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="HotkeyTrayView" Visibility="Collapsed">
                    <StackPanel Padding="36,28" Spacing="22" MaxWidth="720" HorizontalAlignment="Left">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Hotkey &amp; Tray" Style="{ThemeResource TitleTextBlockStyle}" />
                            <TextBlock Text="Open the quick scheduler without leaving what you are doing." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock Text="Quick scheduler hotkey" />
                            <TextBox x:Name="PaletteHotkeyInput" PlaceholderText="Ctrl+Alt+S" />
                            <TextBlock Text="Use at least one modifier plus a letter, number, or F1-F24." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <ToggleSwitch x:Name="StartupToggle" Header="Start the companion when I sign in" />
                        <TextBlock Text="The tray icon keeps the hotkey available and provides quick access to SDAT." TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <Button Content="Save settings" Style="{ThemeResource AccentButtonStyle}" Click="OnSaveSettings" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="AdvancedView" Visibility="Collapsed">
                    <StackPanel Padding="36,28" Spacing="20" MaxWidth="720" HorizontalAlignment="Left">
                        <TextBlock Text="Advanced" Style="{ThemeResource TitleTextBlockStyle}" />
                        <TextBlock Text="SQLite is authoritative. Windows Task Scheduler is repaired from this local state." TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button Content="Reconcile tasks" Click="OnReconcile" />
                            <Button Content="Refresh status" Click="OnRefresh" />
                        </StackPanel>
                        <TextBlock x:Name="DatabasePathText" TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" />
                    </StackPanel>
                </ScrollViewer>

                <ScrollViewer x:Name="AboutView" Visibility="Collapsed">
                    <StackPanel Padding="36,28" Spacing="12" MaxWidth="720" HorizontalAlignment="Left">
                        <TextBlock Text="SDAT" Style="{ThemeResource TitleTextBlockStyle}" />
                        <TextBlock Text="Local, open-source Windows power scheduling without accounts or telemetry." TextWrapping="Wrap" />
                        <TextBlock Text="CLI and terminal UI remain first-class clients of the same C# core." TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </ScrollViewer>

                <InfoBar x:Name="StatusBar" IsOpen="False" IsClosable="True" VerticalAlignment="Bottom" Margin="24" />
            </Grid>
        </NavigationView>
    </Grid>
</Window>
